# Bibliotecas para extração (requests + BeautifulSoup) e validação local (DuckDB/Pandas)
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.1
pandas==3.0.3
duckdb==1.5.0
pyarrow==24.0.0
curl_cffi==0.15.0

# Opcional: só se quiser testar PySpark/Delta Lake localmente, fora do Databricks
# (no Databricks Free Edition o PySpark já vem pronto no notebook, não precisa instalar)
# pyspark==4.1.2
# delta-spark==4.2.0